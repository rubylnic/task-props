class EtsyCard {
  constructor(id, url, img, title, currencyCode, price, quantity) {
    this.id = id;
    this.url = url;
    this.img = img;
    this.title = title;
    this.currencyCode = currencyCode;
    this.price = price;
    this.quantity = quantity;
  }
}
export default EtsyCard;